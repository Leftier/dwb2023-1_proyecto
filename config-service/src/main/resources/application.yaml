server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Leftier/dwb2023-1_config-data.git
          search-paths: api
          default-label: "main"

  security:
    user:
      name: root
      password: admin